# OrbitRouter
Orbit Router – free programmable gateway for AI dev. Connect Kiro AI and ChatGPT accounts, route via unified REST/WebSocket. No API keys in code – Orbit handles auth and session isolation. Cloud-routed, not local. Switch providers without rewriting scripts. For devs, not consumers. Lightweight, stateless, vendor-agnostic.
